<?xml version= "1.0" encoding = "UTF-8"?>

<cronentries>
	
<!--  	<cron>
	    <url>/dailyemails</url>
	    <description>Mail out each day's release date reminders</description>
	    <schedule>every 1 minutes</schedule>
	</cron>  -->
<!-- 	<cron>
	    <url>/helloworld</url>
	    <description>Prints hello world.</description>
	    <schedule>every 1 minutes</schedule>
	</cron> -->
	<cron>
    	<url>/retry</url>
    	<description>Retry on jsdk</description>
	    <schedule>every 10 minutes</schedule>
	    <retry-parameters>
	      <min-backoff-seconds>2.5</min-backoff-seconds>
	      <max-doublings>5</max-doublings>
	    </retry-parameters>
  	</cron>
</cronentries>

<!--TODO: Add security so no one can access the dailyreminders URL except Lighthouse -->